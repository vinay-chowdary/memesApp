{
  "name": "server",
  "version": "1.0.0",
  "description": "api for memes submitted",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix ../client",
    "server": "nodemon server",
    "client": "npm run start --prefix ../client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "keywords": [
    "memes"
  ],
  "author": "vinay chowdary",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.15",
    "node-fetch": "^2.6.1",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
